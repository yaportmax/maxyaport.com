[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "25"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
